; NAME=gigtodo.net
display_errors = off
log_errors = on

; Session cookie hardening -- applies site-wide since session_start() is
; called from hundreds of individual files with no shared bootstrap.
session.cookie_httponly = 1
session.cookie_secure = 1
session.cookie_samesite = "Lax"
